export const metadata = {
  title: "The Connect Retreat 2026",
  description: "Ministers and Workers Retreat Registration",
};

import "./globals.css";

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}